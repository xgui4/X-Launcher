pkgname=X-Launcher-Core
pkgver=git
pkgrel=1
pkgdesc="A Cross-Platform Minecraft Launcher"
arch=('x86_64')
url="https://github.com/xgui4/x-launcher-core"
license=('MIT') 
depends=(
    'dotnet-runtime-9.0'
    'python'
    'pyside6'
    'tk'
    "python-platformdirs"
)
makedepends=(
    'dotnet-sdk-9.0'
    'git'
)
source=('x-launcher-core::git+https://github.com/xgui4/x-launcher-core#branch=master')

sha256sums=('SKIP')

pkgver() {
  cd "$pkgname"
  printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short=7 HEAD)"
}

build() {
  cd "${srcdir}/${pkgname}"
  # To be updated
}

package() {
  cd "${srcdir}/${pkgname}"
  # To be updated
}
