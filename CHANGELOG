# Changelog

## March 29 - Part 2

- Update The new ui to connect the launcher
- Fix the backend
- Add a about section and browser dialog witt their respective changes
- More info in the main `__init__.py` file
- Bug fixes and minors adjustemens 

## March 29 

- Updated .NET codebase and library 
- Updated the scripts for the Python Frontend
- Formatting of the Python Code Base and fix the pyproject.toml file for Pylance
- Update the version

## March 28

- fixed Qt Ressources
- added a WIP UI file for the launcher

## Feb 9 

Add initial project structure and setup scripts

- Created resource file `ressources.qrc` for asset management.
- Added French language support with `French.isl`.
- Implemented Inno Setup script `setupv2.iss` for Windows installer.
- Added build script `build.sh` for backend and frontend compilation.
- Created clean, install, and package scripts for project management.
- Established configuration structure in `src/frontend/config`.
- Developed configuration handling in `src/frontend/utils/config.py`.
- Implemented main application logic in `src/x-launcher-core.py`.
- Initialized project file for KDevelop with `x-launcher.kdev4`.

and more! :)

## Feb 8 : 
new wip setup.py file, updated wip taskfile file , re add uv.lock and remove old  run file (cause it is now obselete and will need full reworked) and added WIP changes to the packages

## January 25 2026 - 15:00

- Updated the `.gitignore`
- Update the Launcher Service
- Update the Launcher Core
- Add a demo launcher
- Updated the README
- Remove versions files

## January 25 2026 - 12:37

- Removed UV.lock since It is not used and won't work propely on FreeBSD due to Pyside6 binary not existing in pip for FreeBSD
- Removed setup.py since it was not really needed
- Update the README

## Restructure the python frontend

- Refactor the pyhon code with packages and python package `__init__.py`
- Updated the launcher with this new structure in mind

## Start of the new structure

- modified the project structure and the code associated
- the readme will be updated next commit
Second commit -> Update the README
Third commit -> update the README again and `pyproject.toml` and new assets (`banner.png`)

## Fixed some bugs of imports + better legacy launcher- develop branch - 2026/01/21 - 21:21

## New-FrontEnd (prototype 2)- develop branch - 2026/01/21

- fixed scripts
- updatd the readme with even more info
- updated loggin with a Basic Logger Class
- moved and updated both launchers.py scripts
- updated pyproject.toml again with setting for basedpyright and new depency
- code refactoring with basedpyright again and formated with black formatted

## New-FrontEnd (prototype 2)- develop branch - 2026/01/20

- fixed scripts
- updatd the readme
- fixed some bugs
- updated the locales
- implemented a start of logging for scripts
- fixed start-legacy launcher
- updated pyproject.toml
- updated UV automatically
- code refactoring plus aware to pyright

## New-FrontEnd (prototype 2)- develop branch - 2026/01/19 - commit 2

- replce install.taskfile with setup.py

## New-FrontEnd (prototype 2)- develop branch - 2026/01/19

- Beginning the refactor of the core backend.
- Added an experimentation with UV
- Removed the `requirement.txt` and move its content on `pyproject.toml`
- Added more info to the `pyproject.toml`
- Added a `.run` folder for JetBrain Rider
- Updated the `.gitignore`
- The project now use a Python venv for the frontend
- Reworked the whole `start-x-launcher.py` launcher scripts
- Make the old project (legacy) an option, now default to the QT
- Update the `.gitignore`
- added `uv.lock` (generated by uv) as the Python Env generator file
- Renamed the `frontend` folder to the `src` folder
- Updated the Solution file

## New-FrontEnd (prototype 2) - started at 2026-01-16 ended on 2026-01-18

- Update the readme
- Added a `global.json` file
- Added a start of `taskfile.yml`
- A lot of minors new features/changes for the MSBUILD and solution file
- Added a taskfile installer to get its latest version
- Renamed `pkg/freebsd-ports` to `pkg/freebsd/`
- `.gitignore` updated
- now the project is configured to be launch and build on target/
- Major changes to the frontends
- Locales and assets from project roots to `frontend/assets` and `frontend/locales`
- Removed the `english.isl`  in `pkg/windows/` since it is not needed
- moved `pkg/windows/french.isl` in `pkg/windows/languages/french.isl`
- Added French documentions and setting for the Windows Installer
- Added `frontend/utils.py`a script used to create a file that contain the path (temporaly workaround)
- Added the legacy backend as a based for the new backend and renamed the old backend as `X_Launcher.Core.Legacy`

## New-FrontEnd (prototype) - 2026-01-15

- Renamed a lot of project for consistency
- Moved the old project to Legacy folder
- New frontend on frontend in PySide6
- Added Locales at `locales` folder
- New config file (`config.ini`) (not used currently)
- Renamed solution (.sln file) from `X-Launcher.sln` to `X_Launcher.sln`
- Some minors bug fixed and better shebangs
- Added package build (arch package system) squeleton
- Modified Windows Inno Setup File Script
- Added start of manifest for freebsd support
- Global Assets are now in `assets/`
- Added `start-x-launcher-app.py`
- Added Locales on `locales/` for the Qt Backend and 2 `.isl` (not used currently)
- Started to modify the readme for the refactor/rewrite
- Added `Directory.Packages.props`
- Added python project file (`pyproject.toml` and `requirements.txt`)
- Updated the `.gitginore`
- Updated the code of respect `code-of-conduct.md`
